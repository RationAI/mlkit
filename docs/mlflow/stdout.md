# Stream Capture

The `stdout` is a stream that is used to output data to the console. It is a standard output stream that is used by the `print()` function in Python.

```python
from rationai.mlkit import StreamCapture


with StreamCapture() as stream:
    ...
```